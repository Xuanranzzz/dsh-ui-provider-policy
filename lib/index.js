/** Host loader entry for the browser implementation exported from `./client`. */
/** Host plugin body — no host-side behavior for the provider-policy settings plugin. */
function apply() {}
export { apply };
